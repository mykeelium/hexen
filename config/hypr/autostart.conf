# Autostart applications
exec-once = hypridle
exec-once = mako
exec-once = waybar
exec-once = hyprpaper

# Tray applets
exec-once = nm-applet --indicator
exec-once = blueman-applet

# Polkit authentication agent (try multiple options)
exec-once = /usr/libexec/lxpolkit || /usr/libexec/polkit-kde-authentication-agent-1 || /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Clipboard manager
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Slow app launch fix -- set systemd vars
exec-once = systemctl --user import-environment $(env | cut -d'=' -f 1)
exec-once = dbus-update-activation-environment --systemd --all

# Keyring for secrets (Signal, etc.)
exec-once = /usr/bin/gnome-keyring-daemon --start --components=secrets
